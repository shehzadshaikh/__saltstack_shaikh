# -*- coding: utf-8 -*-
# vim: ft=sls

banner:
    config:
      filename: /etc/banner.net
      source: salt://banner/files/banner.net
      
    service: 
      name: sshd
      enable: true
      reload: true

    sshd_config:
        filename: /etc/ssh/sshd_config
