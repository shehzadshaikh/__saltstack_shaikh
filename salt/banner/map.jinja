
# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "banner/files/defaults.yaml" as defaults %}

{# Merge in sshd:lookup pillar #}
{% set banner_settings = salt['pillar.get']('banner:lookup', default=defaults.banner, merge=True) %}
