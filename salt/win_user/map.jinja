# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "win_user/files/defaults.yaml" as defaults %}

{# Merge in win_user:lookup pillar #}
{% set user_settings = salt['pillar.get']('win_user:lookup', default=defaults.win_user, merge=True) %}
