# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "localusers/files/defaults.yaml" as defaults %}

{# Merge in users:lookup pillar #}
{% set localusers_settings = salt['pillar.get']('localusers:lookup', default=defaults.localusers, merge=True) %}
