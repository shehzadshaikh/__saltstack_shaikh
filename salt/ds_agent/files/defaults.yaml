# -*- coding: utf-8 -*-
# vim: ft=yaml

dsagent:
    agent:
      version: 11.0
    pkg:
      downloadable: Agent-RedHat
      installable: Agent-Core-RedHat
    service: 
      name: ds_agent
      enable: true
    config:
      filename: /opt/ds_agent/
      source: salt://ds_agent/files/