# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "logrotate/defaults.yaml" as defaults %}

{# Merge in logrotate:lookup pillar #}
{% set logrotate_settings = salt['pillar.get']('logrotate:lookup', default=defaults.logrotate, merge=True) %}