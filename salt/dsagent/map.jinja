# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "dsagent/files/defaults.yaml" as defaults %}

{# Merge in dsagent:lookup pillar #}
{% set dsagent_settings = salt['pillar.get']('dsagent:lookup', default=defaults.dsagent, merge=True) %}
