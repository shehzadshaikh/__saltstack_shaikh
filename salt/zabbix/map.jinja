# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "zabbix/files/defaults.yaml" as defaults %}

{# Merge in zabbix:lookup pillar #}
{% set zabbix_settings = salt['pillar.get']('zabbix:lookup', default=defaults.zabbix, merge=True) %}
