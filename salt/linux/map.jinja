# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "linux/files/defaults.yaml" as defaults %}

{# Merge in linux:lookup pillar #}
{% set linux_settings = salt['pillar.get']('linux:lookup', default=defaults.linux, merge=True) %}
