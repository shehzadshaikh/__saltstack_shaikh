# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "java/files/defaults.yaml" as defaults %}

{# Merge in linux:lookup pillar #}
{% set java_settings = salt['pillar.get']('java:lookup', default=defaults.java, merge=True) %}
