# -*- coding: utf-8 -*-
# vim: ft=jinja

{# This is a barebones map.jinja file #}
{# We import default values that are then merged with pillar data #}

{# Imports defaults #}
{% import_yaml "nexus_repo/files/defaults.yaml" as defaults %}

{# Merge in sshd:lookup pillar #}
{% set nexus_repo_settings = salt['pillar.get']('nexus_repo:lookup', default=defaults.nexus_repo, merge=True) %}
